services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: argon
      POSTGRES_USER: argon
      POSTGRES_PASSWORD: argon_secret
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
